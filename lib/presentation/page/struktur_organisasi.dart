import 'package:flutter/material.dart';

class StrukturOrganisasi extends StatelessWidget{
  const StrukturOrganisasi({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: 
      Center(child: const Text("Strukturorganisasi"),)
      ),
    );
  }
}